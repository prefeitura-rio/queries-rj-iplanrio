version: 2

models:
  - name: raw_os_info_common__dc_unidade_federacao
    description: "Tabela que armazena as possíveis unidades federativas no preenchimento de um endereço. DML executada direto no banco de dados."
    columns:
      - name: uf_ibge_codigo
        description: "Código IBGE da unidade federativa"
        #datatype: string
      - name: uf_sigla
        description: "Sigla da unidade federativa"
        #datatype: string
      - name: uf_descricao
        description: "Descrição/nome da unidade federativa"
        #datatype: string
      - name: datalake_loaded_at
        description: "Timestamp de quando o dado foi extraído do banco de dados de origem do OSINFO."
        data_type: timestamp
      - name: datalake_transformed_at
        description: "Timestamp de quando o dado foi transformado no dbt."
        data_type: timestamp
