version: 2

models:
  - name: raw_plano_educacional_individualizado__grupamento_disciplina
    description: |
      **Origem**: GestaoEscolar.dbo.PEI_GrupamentoDisciplina
      Grupamentos da Disciplina
    columns:
      - name: id_grupamento
        description: "Identificador do grupamento"
        data_type: int64

      - name: id_disciplina
        description: "Identificador da disciplina"
        data_type: int64

      - name: loaded_at
        description: "Timestamp de quando o dado foi extraído do banco de dados de origem."
        data_type: timestamp

      - name: transformed_at
        description: "Timestamp de quando o dado foi transformado no dbt."
        data_type: timestamp
