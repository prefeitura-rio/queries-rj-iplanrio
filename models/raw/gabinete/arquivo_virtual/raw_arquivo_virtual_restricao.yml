version: 2

models:
  - name: raw_arquivo_virtual_restricao
    description: "Tabela auxiliar (dimensão) com os dados de restrições aos acervos"
    config:
      alias: restricao_acesso
      
      
    columns:
      - name: codigo_restricao
        description: "PK desta tabela – Identificador único da condição de restrição de acesso ao acervo (Campo auto incremental)"
        data_type: int64
        # data_tests:
        #   - unique:
        #       name: raw_arquivo_virtual_restricao__id_restricao__unique
        #       severity: error
        #   - not_null:
        #       name: raw_arquivo_virtual_restricao__id_restricao__not_null
        #       severity: error
      - name: restricao_basica
        description: "Descrição da restrição (ex: Sem restrições, Com restrições)"
        data_type: string
        quote: true
