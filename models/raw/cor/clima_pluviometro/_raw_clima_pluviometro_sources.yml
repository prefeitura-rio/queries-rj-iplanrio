version: 2

sources:
  - name: brutos_clima_pluviometro
    database: rj-cor
    schema: clima_pluviometro
    tables:
      - name: taxa_precipitacao_alertario_5min
    config:
      freshness: # default freshness
        error_after: {count: 24, period: hour}
      loaded_at_field: data_medicao