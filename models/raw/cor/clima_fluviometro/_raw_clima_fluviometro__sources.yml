version: 2

sources:
  - name: clima_fluviometro_staging
    database: rj-iplanrio
    schema: clima_fluviometro_staging
    tables:
      - name: lamina_agua_inea
    config:
      freshness:
        error_after: {count: 24, period: hour}
      loaded_at_field: data
