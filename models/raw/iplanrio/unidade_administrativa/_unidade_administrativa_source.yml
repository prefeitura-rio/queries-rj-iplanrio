version: 2

sources:
  - name: brutos_sici_staging
    database: rj-iplanrio 
    schema: brutos_sici_staging
    tables:     
      - name: Vw_UA
        description: "Tabela de unidades administrativas"
      - name: vw_DataLake
        description: "View combinando tabelas sici.t_ug_atual, sici.ug_tipo_ug, estrutura_batch, tipo_equipamento, horario, endereco e comunicacao para formar a tupla a ser utilizada pelo DataLake"

    config:
      freshness: # default freshness
        error_after: {count: 24, period: hour}
      loaded_at_field: _airbyte_extracted_at
