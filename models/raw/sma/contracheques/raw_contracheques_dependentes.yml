version: 2

models:
  - name: raw_contracheques_dependentes
    description: "Tabela contendo os registros de DEPENDENTES do sistema de contracheques"
    columns:
      - name: numero
        description: "NUMERO"
        data_tests:
          - not_null:
              name: raw_contracheques_dependentes__numero__not_null

      - name: numfunc
        description: "NUMFUNC"
        data_tests:
          - not_null:
              name: raw_contracheques_dependentes__numfunc__not_null

      - name: cpf
        description: "CPF"

      - name: nome
        description: "NOME"

      - name: datalake_loaded_at
        description: "Timestamp de quando o dado foi extraído do banco de dados de origem."
        data_type: timestamp
      - name: datalake_transformed_at
        description: "Timestamp de quando o dado foi transformado no dbt."
        data_type: timestamp
